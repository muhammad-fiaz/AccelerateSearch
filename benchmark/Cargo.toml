[package]
name = "accelerate-benchmark"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "evaluate"
path = "evaluate.rs"
